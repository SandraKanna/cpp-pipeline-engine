# Inherits the root config; disables magic-numbers because in tests
# literal values are the test data itself, not constants to be named.
InheritParentConfig: true
Checks: >
  -readability-magic-numbers